using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;

namespace ClassWork
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //We're trying to do class work because I literally have no fucking clue what's 
            //happening in C#

            //arrays need to be declared as a CONSTANT because reasons...
            //array will only use a set size |(and then doesn't lus to change 
            //because fuck you)

            //to lengthen array, use the "NEW" key word
            //but then apparently the data of the old array is gone...?

            //all functions in ArrayLists are in Arrays (class), they just gatekept it
            //Uncle Rodolph avoids double arrays...I think that's what he said

            //use "new" to make two copies of the same kak in memory (with objects)
            //we're testing to see if it also works with array



            //you can use "var" in arrays too
        }
    }
}
